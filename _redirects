# Redirect www to non-www (configure in Cloudflare Dashboard instead)
# Redirect .pages.dev to custom domain (configure in Cloudflare Dashboard instead)

# All other paths serve index.html
/index.html /index.html 200
